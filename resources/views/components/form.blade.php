<!-- <form action="{{$action}}" method="post">
        @csrf
        @if(isset($nome))
        @method('PUT')
        @endif
        <div class="mb-3">
            <label for="nome" class="form-label">Nome:</label>
            <input type="text"
             id="nome" 
             name="nome" 
             class="form-control"
             @if(isset($nome))value="{{$nome}}" @endif
              >
        </div>
        <button type="submit" class="btn btn-primary">Adicionar</button>
    </form>


 -->

    